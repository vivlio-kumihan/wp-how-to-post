import { readMetadata, VFM, } from '@vivliostyle/vfm';
import fs from 'node:fs';
import vfile from 'vfile';
export function processMarkdown(filepath, options = {}) {
    const markdownString = fs.readFileSync(filepath, 'utf8');
    const vfm = VFM(options, readMetadata(markdownString));
    const processed = vfm.processSync(vfile({ path: filepath, contents: markdownString }));
    return processed;
}
export function readMarkdownMetadata(filepath) {
    return readMetadata(fs.readFileSync(filepath, 'utf8'));
}
//# sourceMappingURL=markdown.js.map